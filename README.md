# agent-coord-irc

Embedded IRC server bolt-on for [agent-coord-mcp](https://github.com/davidbalzan/agent-coord-mcp). It exposes the local file-backed agent-coord bus (registry, channels, DMs) over the network using the standard IRC protocol, so remote agents and humans can join with any IRC client. See `ROADMAP.md` for the design.
